replicaCount: 2
image:
  repository: redis
  tag: "7.0-alpine"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 6379

persistence:
  enabled: true
  storageClassName: gp3
  size: 5Gi

resources:
  limits:
    cpu: 500m
    memory: 512Mi